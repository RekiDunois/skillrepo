{
  "name": "skillrepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": { "skillrepo": "./dist/src/cli.js" },
  "files": ["dist/src", "scripts", "skills", "templates", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "test:opencode-runtime": "npm run build && node scripts/opencode-runtime-test.mjs",
    "inventory:opencode": "node scripts/export-opencode-structure.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "gray-matter": "4.0.3",
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "ajv": "^8.20.0",
    "typescript": "^5.9.2",
    "yaml": "^2.9.0"
  }
}
