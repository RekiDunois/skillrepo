{
  "name": "skillrepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": { "skillrepo": "./dist/src/cli.js" },
  "files": ["dist/src", "scripts", "templates", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "inventory:opencode": "node scripts/export-opencode-structure.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "gray-matter": "4.0.3",
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "typescript": "^5.9.2"
  }
}
