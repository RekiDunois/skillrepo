{
  "name": "skillrepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": { "skillrepo": "./dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/**/*.test.js"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "jsonc-parser": "^3.3.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "typescript": "^5.9.2"
  }
}
